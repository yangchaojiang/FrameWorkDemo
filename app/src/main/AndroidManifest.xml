<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.d.yangc.framework">

    <application
        android:name=".java.app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".kotlin.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".java.interfaceLifecycleActivity" />
        <activity android:name=".java.TestLifecycleActivity" />
        <activity android:name=".kotlin.recyclerView.RecyclerViewActivity" />
        <activity android:name=".kotlin.data.Main7Activity" />
        <activity android:name=".kotlin.steram.FIleSteamActivity" />
        <activity android:name=".java.steam.JavaFileActivity" />
    </application>

</manifest>